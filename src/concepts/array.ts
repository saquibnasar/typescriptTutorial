const superHero: string[] = [];

const way2superHero: Array<string> = [];

type arrayUser = {
  name: string;
  isActive: boolean;
};
const allUser: arrayUser[] = [];

const mlModals: number[][] = [[2], [3], []];

superHero.push("ironMan");
way2superHero.push("ironMan");

allUser.push({ name: "hell", isActive: true });
